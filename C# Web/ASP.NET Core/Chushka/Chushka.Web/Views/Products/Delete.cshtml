@model Chushka.Common.Models.Products.DeleteProductInputModel

@{
    ViewData["Title"] = "Delete Product";
}

<div class="mt-3">
    <h1 class="text-center">@ViewData["Title"]</h1>
    <hr class="hr-2 bg-dark" />
    <form asp-controller="Products" asp-action="Delete" method="post" class="mx-auto half-width">
        <div class="form-group">
            <label asp-for="Name">Name</label>
            <input asp-for="Name" class="form-control" placeholder="Name..." disabled>
        </div>
        <div class="form-group">
            <label asp-for="Price">Price</label>
            <input asp-for="Price" step="0.01" min="0" class="form-control" placeholder="Price..." disabled>
        </div>
        <div class="form-group">
            <label asp-for="Description">Description</label>
            <textarea asp-for="Description" class="form-control" placeholder="Description..." disabled></textarea>
        </div>

        <h5 class="text-center">Product Type</h5>
        <hr class="hr-2 bg-dark half-width" />
        <div class="mt-4 d-flex justify-content-around ">
            @foreach (var typeName in Enum.GetNames(Model.Type.GetType()))
            {
                if (Model.Type.ToString() == typeName)
                {
                    <div class="form-check form-check-inline">
                        <input asp-for="Type" class="form-check-input" type="radio" value="@typeName" checked disabled>
                        <label asp-for="Type" class="form-check-label">@typeName</label>
                    </div>
                }
                else
                {
                    <div class="form-check form-check-inline">
                        <input asp-for="Type" class="form-check-input" type="radio" value="@typeName" disabled>
                        <label asp-for="Type" class="form-check-label">@typeName</label>
                    </div>
                }
            }
        </div>
        <hr class="hr-2 bg-dark half-width" />
        <div class="button-holder d-flex justify-content-center">
            <button type="submit" class="btn chushka-bg-color" mx-5>Delete</button>
            <a asp-area="" asp-controller="Home" asp-action="Index" class="btn chushka-bg-color mx-5">Cancel</a>
        </div>
    </form>
</div>